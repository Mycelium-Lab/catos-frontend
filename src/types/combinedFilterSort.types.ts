export type CreditorLoansSort = {
    amount: boolean;
    duration: boolean;
    id: boolean;
    name: boolean
};

export type CreditorLoansFilter = {
    actual: boolean;
    approved: boolean;
    pending: boolean;
};

export type SelectedCreditorLoansSort = {
    amount: boolean;
    duration: boolean;
    id: boolean;
    name: boolean
};

export type SelectedCreditorLoansFilter = {
    actual: boolean;
    approved: boolean;
    pending: boolean;
};


